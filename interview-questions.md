# 1. Introduction to Java
1. What is Java? → Java is a high-level, platform independent, object-oriented, class-based programming language developed by James Gosling at Sun Microsystems in 1995.
2. Why is Java platform independent? → Java is platform independent because the Java compiler converts source code into platform-independent bytecode. This bytecode is executed by the JVM specific to each operating system.
3. What is JVM? → JVM is the Java Virtual Machine. It is responsible for executing Java bytecode by converting it into machine code. Each operating system has its own JVM.
4. What is JRE? → JRE is the Java Runtime Environment that provides the environment required to run Java applications. It contains the JVM and the required libraries and supporting files.
5. What is JDK? → JDK is the Java Development Kit which is used for developing Java applications. It contains the JRE, compiler (`javac`), debugger, and other development tools.
6. Difference between JVM, JRE and JDK. → JVM: Executes bytecode, JRE: provides environment for apps to run and includes JVM + Libraries, JDK: used to develop java apps, includes JRE + Development tools (compiler, debugger, etc.)
7. How does Java code execute? → The way it executes, first the programmer writes a .java file, then the compiler converts this high language code to (.class) byte code, the bytecode is capable of being run on any platform, now the JVM specific to an OS, converts this byte code to the respective machine code language.
8. Why is Java called "Write Once Run Anywhere"? → Because code gets converted into byte code that can be run on any OS, where the respective OS’s JVM converts it.
9. What is bytecode? → Bytecode is the platform-independent intermediate code generated by the Java compiler (`javac`). It is stored in `.class` files and executed by the JVM.
10. What is compilation vs interpretation? → Compilation is the process of converting source code into bytecode. The compiler checks the entire program for syntax errors before generating the output whereas Interpretation executes code line by line instead of generating a separate executable beforehand.
11. Is java a compiled/interpretable language? → Java is both a compiled and an interpreted language. The Java compiler (`javac`) first compiles the source code into bytecode. The JVM then executes this bytecode. Initially, the JVM interprets the bytecode, but for methods or code blocks that are executed frequently, the Just-In-Time (JIT) compiler compiles the bytecode into native machine code. This improves performance because the compiled machine code can be executed directly on subsequent executions.
12. What is classpath? → Classpath is the location where the Java compiler and the JVM look for compiled classes, packages, and libraries required by a Java program. Common locations include the `bin/` directory containing `.class` files and the `lib/` directory containing external JAR libraries.
13. What is main() method? → The main method in java is an indicator to JVM that this is where the program execution must start.
14. Why is main() static? → static so that JVM can call it without object creation of class.
15. Why is String[] args used? → `String[] args` is used to receive command-line arguments passed to the Java program at the time of execution. The JVM automatically stores these arguments as an array of strings and passes them to the `main()` method.
--- 
# 2. Java Basics
1. final keyword → The `final` keyword is used to indicate members that cannot be modified, A final variable cannot be reassigned, A final method cannot be overridden, A final class cannot be inherited.
2. Comments in Java → multiline comments are given as /* */, for documentation /** */, for single line comments //
3. Identifiers → Identifiers are the names given to variables, methods, classes, interfaces, packages, and other program elements.
4. Keywords → keywords are reserved words in java that cannot be used as variable/method names
5. Naming conventions → class: PascalCase (Student), variable & method: camelCase (studentName), constant: UPPER_SNAKE_CASE (MAX_SIZE), package: lowercase (com.company.project)
6. Why String is not primitive? → String is not primitive because it is a class in Java. Since it is a class, String objects can have methods and behavior such as `length()`, `substring()`, and `charAt()`, unlike primitive data types.
7. Difference between source file and class file → source file has a .java extension, and it is written in a high level language, class file has a .class extension and it contains bytecode
8. Can we have multiple classes in one file? → Yes, a Java source file can contain multiple classes, but only one public class is allowed, and its name must match the filename.
9. Can a Java file have multiple main methods? → Yes, a Java class can have multiple overloaded main() methods, but the JVM starts execution only from public static void main(String[] args).
---
# 3. Data Types, Variables & Input

Primitive data types store the actual value directly, whereas non-primitive data types store a reference (memory address) to an object. Local primitive and reference variables are typically stored on the stack, while the actual objects are created on the heap.

1. Primitive data types → The 8 primitive data types are (byte-1, short-2, int-4, long-8, float-4, double-8, char-2, boolean-1)
2. Non-primitive data types →Example: String, Arrays, Classes, Interfaces, Objects.
3. Why char occupies 2 bytes? → Java uses the UTF-16 Unicode character encoding, so each `char` occupies 2 bytes (16 bits).
4. Default values → primitives have default value 0/false and non primitives have default value, null.
5. Variable declaration → data_type var_name;
6. Initialization → data_type var_name = value;
7. Type casting → conversion of one data type into another
8. Implicit casting (widening) → conversion of smaller to larger datatype coercion eg → int a = 23; double  b = a;
9. Explicit casting (narrowing) → conversion of larger to smaller datatype eg → double a = 2345; int b = (int) a
10. Overflow → When a value exceeds the maximum range of a data type, it wraps around. Example: byte b = 127; → b++;  → // -128
11. Underflow → When a value goes below the minimum range of a data type, it wraps around. Example: byte b = -128 ; → b--; → // 127 : for float underflow to 0.0
12. Unicode → Universal character encoding standard that assigns a unique code pointing to characters from almost every writing system. Java stores `char` values using UTF-16.
13. ASCII → “American Standard Code for Information Interchange” is a 7-bit character encoding standard that represents English letters, digits, and symbols using numeric values (0–127).
14. Wrapper classes → Wrapper classes convert primitive data types into objects. eg: int: Integer
15. Autoboxing → Automatic conversion of a primitive to its corresponding wrapper object.
16. Unboxing → Automatic conversion of a wrapper object back into its primitive value.
17. Scanner class → Scanner class is a predefined class in java.util that is used to read input from the keyboard, files or other input streams. Example: Scanner sc = new Scanner(System.in); String name = sc.nextLine();
18. Buffered Reader → Buffered Reader is a class in java.io, that is used to read text efficiently by buffering characters, it is faster than Scanner since it doesn’t parse input into different data types automatically. Example: BufferedReader br = new BufferedReader(new InputStreamReader(System.in)); String name = br.readLine();
---
